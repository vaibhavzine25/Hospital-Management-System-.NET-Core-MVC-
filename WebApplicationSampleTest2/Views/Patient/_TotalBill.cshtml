@model Dictionary<string, string>

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table align-middle table-nowrap table-hover">
                        <thead class="table-light">
                            <tr>
                                <th scope="col">Type</th>
                                <th scope="col">Amout</th>
                                <th scope="col">Total</th>
                                <th scope="col">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null)
                            {
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @item.Key
                                        </td>
                                        <td>
                                            @item.Value
                                        </td>
                                        <td>
                                            @ViewData["TotalBill"]
                                        </td>
                                        <td>
                                            <a class="btn btn-sm btn-danger" onclick="deletepreAuthCommand('@item.Key')">delete</a>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>